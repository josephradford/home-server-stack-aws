---
# Top bar widgets
- logo:
    icon: mdi-shield-home

- search:
    provider: duckduckgo
    target: _blank

- datetime:
    text_size: xl
    format:
      dateStyle: long
      timeStyle: short
      hour12: false

# System resources
- resources:
    label: Server
    cpu: true
    memory: true
    uptime: true
    # Note: cputemp disabled - EC2 instances don't expose temperature sensors
    # Note: disk monitoring removed - doesn't work reliably with Docker bind mounts
