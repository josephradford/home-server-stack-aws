# AWS Deployment Configuration
# Simplified environment configuration for AWS EC2 deployment
# Copy this to .env and update with your values

# =============================================================================
# DOMAIN CONFIGURATION
# =============================================================================

# Your domain name (should point to EC2 Elastic IP via Route53)
# Example: myapp.example.com or aws.example.com
DOMAIN=aws.example.com

# Email for Let's Encrypt SSL certificates
ACME_EMAIL=admin@example.com

# =============================================================================
# SERVICE CREDENTIALS
# =============================================================================

# n8n - Workflow Automation
# Used for basic authentication to protect the n8n editor
N8N_USER=admin
N8N_PASSWORD=change_this_secure_password

# Traefik Dashboard
# Password will be hashed during setup - use: htpasswd -nb admin yourpassword
TRAEFIK_PASSWORD=change_this_secure_password
# After running setup, this will be populated:
TRAEFIK_DASHBOARD_USERS=

# =============================================================================
# SYSTEM CONFIGURATION
# =============================================================================

# Timezone (e.g., America/New_York, Australia/Sydney, Europe/London)
TIMEZONE=UTC

# User/Group IDs (usually 1000 for first non-root user)
# Run `id -u` and `id -g` to get your values
PUID=1000
PGID=1000

# =============================================================================
# N8N CONFIGURATION
# =============================================================================

# Task execution timeouts (in seconds)
N8N_RUNNERS_TASK_TIMEOUT=1800
EXECUTIONS_TIMEOUT=1800
EXECUTIONS_TIMEOUT_MAX=3600

# =============================================================================
# HOMEPAGE DASHBOARD CONFIGURATION
# =============================================================================

# BOM Weather - Australian Bureau of Meteorology
# Uses custom homepage-api backend (no API key needed)
# Suburb name only (e.g., "parramatta", "sydney", "melbourne")
BOM_LOCATION=parramatta

# =============================================================================
# AWS-SPECIFIC NOTES
# =============================================================================

# CloudWatch Monitoring:
# Install CloudWatch agent on EC2 for metrics and logs
# Follow: https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/install-CloudWatch-Agent-on-EC2-Instance.html
#
# Security Groups:
# - Port 22 (SSH) - Your IP only
# - Port 80 (HTTP) - 0.0.0.0/0 (redirects to HTTPS)
# - Port 443 (HTTPS) - 0.0.0.0/0
#
# Route53 DNS:
# Create A record: ${DOMAIN} -> EC2 Elastic IP
# Create CNAME: *.${DOMAIN} -> ${DOMAIN}
#
# Backup Strategy:
# - Daily EBS snapshots
# - Or manual: tar -czf backup.tar.gz data/ .env
# - Store in S3 bucket for disaster recovery
